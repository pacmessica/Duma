<%= form_for @contact do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :translation %>
    <%= f.text_field :translation %>

    <%= f.submit (@study_word.new_record? ? "Create" : "Update") %>

<% end %>
